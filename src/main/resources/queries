create table user_credential (username varchar(20), password varchar(20), roles varchar(20) )
insert into USER_CREDENTIAL (USERNAME,PASSWORD, ROLES) values ('Abhideep', 'bakshi', 'ROLE_ADMIN;ROLE_USER' )
select * from USER_CREDENTIAL